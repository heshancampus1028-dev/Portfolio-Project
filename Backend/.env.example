# Copy this file to ".env" and fill in your own values

# Local MongoDB (make sure mongod is running), or use a MongoDB Atlas URI
MONGO_URI=mongodb://localhost:27017/portfolioDB

# Port the Express server will run on
PORT=5000
